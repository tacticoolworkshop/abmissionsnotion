const { EmbedBuilder, ButtonBuilder, ButtonStyle} = require('discord.js')

module.exports = {
    guildId: process.env.GUILD_ID,
    channel: process.env.ANNOUNCE, 
    adminID: '123456789',
    flags: {
        
    }
}
    

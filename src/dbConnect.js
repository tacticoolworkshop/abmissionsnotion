/*const mongoose = require('mongoose'); 
const uSchema = require('../../wsBot/src/models/levels'); 

const connection2 = mongoose.createConnection(process.env.MONGODB_URI);
const Users = connection2.model('Users', uSchema);

const dbConnect = async () => {
    return { Users };
}

module.exports = dbConnect;*/